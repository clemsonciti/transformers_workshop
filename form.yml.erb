# **MUST** set cluster id here that matches cluster configuration file located
# under /etc/ood/config/clusters.d/*.yml

cluster: "palmetto2"

attributes:

  reservation:
    label: "Use Workshop Reservation?"
    widget: "select"
    required: true
    value: "Yes"
    options:
      - ["Yes"]
      - ["No"]
  
form:
  - reservation
  - bc_email_on_started
